/target
/dist

# `claude mcp add` writes this with an absolute, machine-specific
# binary path. See README's "Using it with an MCP client" section for
# the command that regenerates it locally.
/.mcp.json
